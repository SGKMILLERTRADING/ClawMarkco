---
description: Sync local changes to GitHub
---

// turbo-all

1. Stage all changes
```powershell
git add .
```

2. Commit changes with a descriptive message
```powershell
git commit -m "Update openclaw files"
```

3. Push to the main branch
```powershell
git push origin main
```
